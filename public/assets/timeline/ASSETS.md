# Timeline home — Figma assets to export

Export these from the Figma frame (node 2176-103056) and **overwrite the same filenames**
in this folder (`public/assets/timeline/`). The code already points here, with placeholder
images in place, so the page renders now and sharpens the moment you drop the real art.

| File | What it is | Export settings |
|------|-----------|-----------------|
| `hero.jpg`        | Hero background photo (woman, purple-toned) behind "Good morning …" | JPG, 2× (~1080px wide), quality ~85 |
| `live-family.png` | "Review family health insights" card background (green-tinted family) | PNG or JPG, 2× (~900px wide) |
| `live-refer.png`  | "Refer your friends and earn $50" card background | PNG or JPG, 2× (~900px wide) |
| `rx-vial.png`     | Cyborg-for-Rx blood-vial image | **PNG with transparency**, 2× (~320px wide) |

How to export in Figma: select the image layer/frame → right panel **Export** → set format/scale → Export.
Keep the exact filenames above and this folder; no code change needed after you drop them.

Everything else in the design (blood-draw calendar icon, steppers, buttons, cards) is drawn in code — no export needed.
